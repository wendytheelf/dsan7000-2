defaults:
  # Geometric dimensions: Your data is typically in mm (common in Qto_* properties)
  length: mm         # Applies to generic "Length" (we will align by dimension)
  width: mm
  depth: mm
  thickness: mm
  diameter: mm

  # If your data already has NetArea/NetSideArea/CrossSectionArea in m2, these can be omitted
  # netarea: m2
  # netsidearea: m2
  # crosssectionarea: m2
